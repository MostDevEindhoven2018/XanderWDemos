@model List<Student>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<table>

    @foreach (Student s in Model)
    {
        <tr>
            <td>@s.StudentID</td>
            <td>@s.Firstname</td>
            <td>@s.Lastname</td>
        </tr>
    }

</table>
